# render.yaml – deploy OpenReel Video on Render

services:
  - type: web
    name: openreel-video
    runtime: static-site
    buildCommand: npm install && npm run build
    publishPath: dist
    envVars:
      - key: NODE_ENV
        value: production